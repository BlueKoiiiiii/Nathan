#extends Camera2D

#var target_player = null

# Called when the node enters the scene tree for the first time.
#func _ready():
	#pass
	#target_player = GameManager.player_master


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
	#pass
#	if GameManager.player_master != null:
#		global_position = GameManager.player_master.global_position
